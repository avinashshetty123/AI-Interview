{
  "installCommand": "if [ -f frontend/package.json ]; then npm install --prefix frontend && PUPPETEER_SKIP_DOWNLOAD=true npm install --prefix backend; else npm install && PUPPETEER_SKIP_DOWNLOAD=true npm install --prefix ../backend; fi",
  "buildCommand": "if [ -f frontend/package.json ]; then npm run build --prefix frontend; else npm run build; fi",
  "outputDirectory": "dist",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/logo.png",
      "destination": "/api/index.js"
    },
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
